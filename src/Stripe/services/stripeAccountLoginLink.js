// const stripe = require('stripe')(process.env.STRIPE_SECRET_KEY);

// module.exports = async function createLoginLink (id) {
//     const loginLink = await stripe.accounts.createLoginLink(id);
//       return loginLink
// }